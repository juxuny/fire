environments:
  jump:
    HOST: 192.168.31.79
    USER: yuanjie
    PORT: 22
    PRIVATE_KEY: ${HOME}/.ssh/id_rsa
version: "v0.0.1"
name: fire-demo
tasks:
- name: all
  type: bash
  env: jump
  scripts:
    - env
    - ls -lha
    - echo $PRIVATE_KEY

dependencies:
- "juxuny/installer:v1.0.0"

replace:
- package: juxuny/installer
  repository: ./installer

