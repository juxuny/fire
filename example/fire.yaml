environments:
  jump:
    HOST: 192.168.31.79
    USER: yuanjie
    PORT: 22
    PRIVATE_KEY: ${HOME}/.ssh/id_rsa
version: "v0.0.1"
name: fire-demo
tasks:
- name: all
  type: bash
  env: jump
  scripts:
    - env
    - ls -lha
    - echo $PRIVATE_KEY

dependencies:
  - "installer" 

replace:
  - package: installer
    repository: ./installer


